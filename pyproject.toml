[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "adaptivevision"
version = "0.0.0"
description = "Industrial 2D/3D Vision, AOI & Metrology Edge Platform"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Proprietary" }
authors = [{ name = "AdaptiveVision Team" }]
keywords = ["computer-vision", "aoi", "metrology", "edge-ai", "industrial"]
# No runtime dependencies at Milestone M0 - the logging bootstrap uses only the
# standard library. Dependencies are added by the milestone that first needs them.
dependencies = []

[project.optional-dependencies]
dev = [
    "ruff>=0.6",
    "mypy>=1.11",
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "pre-commit>=3.7",
]

[tool.hatch.build.targets.wheel]
packages = ["src/adaptivevision"]

# ---------------------------------------------------------------------------
# Ruff: linter + formatter
# ---------------------------------------------------------------------------
[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "scripts", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "C4", "SIM", "RUF", "D", "ANN"]
ignore = [
    "D203", # one-blank-line-before-class (conflicts with D211)
    "D213", # multi-line-summary-second-line (conflicts with D212)
]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.per-file-ignores]
# Tests and scaffold scripts need not carry full docstring/annotation rigor.
"tests/*" = ["D", "ANN"]

# ---------------------------------------------------------------------------
# mypy: strict static typing on production code
# ---------------------------------------------------------------------------
[tool.mypy]
python_version = "3.11"
strict = true
warn_unused_configs = true
warn_redundant_casts = true
warn_unused_ignores = true
files = ["src", "scripts"]

# ---------------------------------------------------------------------------
# pytest + coverage
# ---------------------------------------------------------------------------
[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers --cov --cov-report=term-missing --cov-fail-under=95"

[tool.coverage.run]
source_pkgs = ["adaptivevision"]
branch = true

[tool.coverage.report]
show_missing = true
exclude_lines = ["if __name__ == .__main__.:", "raise NotImplementedError"]
