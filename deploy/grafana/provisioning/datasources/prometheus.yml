# AdaptiveVision Grafana datasource provisioning (Milestone M18).
#
# Registers the Prometheus instance as a Grafana datasource on startup.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
